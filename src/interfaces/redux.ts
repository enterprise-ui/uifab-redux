export interface IProcessState {
  isLoaded?: boolean

  isLoading?: boolean

  isLoadingMore?: boolean

  isLoadingSort?: boolean

  isLoadingFilter?: boolean

  isSaving?: boolean

  isDeleting?: boolean
}
